{
  "name": "bloglist-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "NODE_END_production npm start --prefix backend"
  },
  "dependencies": {
    "concurrently": "^10.0.5",
    "prettier": "^3.9.6"
  }
}
